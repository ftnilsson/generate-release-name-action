export declare class AwesomeReleaseNameGenerator {
    private delimiter;
    private length;
    private appendText;
    private useToken;
    private capitalize;
    constructor(delimiter?: string, length?: number, appendText?: string, useToken?: boolean, capitalize?: boolean);
    generate(): string;
}
