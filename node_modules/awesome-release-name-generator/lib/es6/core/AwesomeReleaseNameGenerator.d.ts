export declare class AwesomeReleaseNameGenerator {
    private delimiter;
    private length;
    private useToken;
    private capitalize;
    constructor(delimiter?: string, length?: number, useToken?: boolean, capitalize?: boolean);
    generate(): string;
}
